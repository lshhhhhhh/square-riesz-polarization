# External submission status

The user reported in this Codex task that the Friedman record-board email had
been sent outside Codex.

What this repository can establish:

- commit `533dcc89dcd62817f0d1afcd670f616cbea8cc3c` contains the reviewed
  pre-send package;
- the package contains nine proposed records (`N=3,5,29..35`), their literal
  coordinates, figures, and reproducible certificates;
- no email was sent by the project scripts or by Codex.

What was not archived and is therefore unknown:

- the exact text that was transmitted;
- the final public credit and signature;
- the exact attachment selection;
- the external send timestamp and message identifier.

The status `USER_REPORTED_EMAIL_SUBMITTED` records the user's report only. It
does not mean that the tracked draft exactly matches the sent message, that the
recipient received it, or that Friedman accepted or published any record.
