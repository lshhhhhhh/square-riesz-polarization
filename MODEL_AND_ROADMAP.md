# 模型、GPU 路线与验证路线

## 1. 三层问题必须分开

### 层 1：离散测试点近似

对有限测试集 `Q` 计算

\[
\widehat P_Q(A)=\min_{q\in Q}\sum_i \lVert q-a_i\rVert^{-2}.
\]

它适合 GPU 搜索，但通常**高估**真实连续域最小值，不能作为记录证明。

### 层 2：数值连续域最小值

从稠密网格的低值点、边界低值点和已有最暗点出发，对 `x` 做多起点局部最小化，聚类得到所有数值局部极小点。它是候选排名与结构分析层，仍不是严格证明。

### 层 3：严格连续下界

对最终有限小数坐标运行精确有理 branch-and-bound，证明整个 `[0,1]^2` 上的势都不低于声明值。优先复用并泛化公开证书库，不重新发明验证框架。

## 2. GPU 应该用在哪里

`N<=52` 时单个构型的求和太小，单独搬到 RTX 5090 不能充分利用 GPU。正确批处理维度是：

- 多个 `N`；
- 数百到数千个候选构型；
- 每个构型数千到数十万测试点；
- 多个温度、对称分支和随机种子。

核心张量可组织为 `batch x test_points x sources x 2`，按显存分块，求距离平方、倒数和、每个构型的最小值。需要记录实际吞吐和显存，再决定是否 `torch.compile` 或写 Triton/CUDA kernel。

不要先做 FMM：在本规模上它会增加复杂度，未必改善总运行时间。

## 3. 候选生成器

至少并行维护以下族，不能只押对称解：

1. `D4`、`D2`、单轴反射、180 度旋转；
2. 完全自由的非对称构型；
3. 裁剪三角晶格 + 边界松弛；
4. CVT/容量约束 Voronoi/blue-noise 初值；
5. `N-1 -> N` 增量插点与 `N+1 -> N` 删除；
6. 整数多重度位置，再允许每个簇分裂；
7. 从公开 incumbent 做有归属的局部精化，用于复现和性能校准，不把同盆地精化当作新构型。

公开结果已显示对称与非对称盆地并存；还出现距离约 `0.005` 或 `0.001` 的近重合点。优化器必须允许碰撞/近碰撞，不能强加最小点间距。

## 4. 平滑探索与精确 active-set 精化

### 粗搜索

对稠密测试网格使用 soft-min：

\[
\operatorname{softmin}_\tau(U(q))
=-\tau\log\sum_q e^{-U(q)/\tau}.
\]

从较大 `tau` 退火到较小 `tau`，用 Adam/LBFGS 优化批量构型。必须同时监测真实 hard minimum，防止只优化平均低值区。

### Exchange loop

1. 用当前 active test points 优化源位置和阈值 `t`；
2. 对当前源位置，在正方形内部和四条边上多起点寻找数值全局最暗点；
3. 把新最暗点加入 active set，淘汰长期不活跃点；
4. 联合移动所有源，避免逐点移动算法的假局部最优；
5. 当违反量与坐标变化都收敛后，转严格证书。

Rolfes–Schüler–Zimmermann 的必要条件作为诊断：局部最优构型应位于最暗点凸包内，并应有边界最暗点。违反条件意味着“数值收敛”不可信。

## 5. KKT/力平衡数据

若活跃最暗点为 `p_j`，外层 max-min 的局部 KKT 条件应产生非负权重 `lambda_j`，`sum lambda_j=1`，使每个自由源的加权梯度平衡：

\[
\sum_j \lambda_j\nabla_{a_i}\lVert p_j-a_i\rVert^{-2}=0,
\]

边界源还要加入法锥项。

数值上可解一个小的非负最小二乘/线性规划来恢复 `lambda`，记录残差。它同时用于：

- 判断 active minima 是否漏找；
- 区分真局部最优与网格伪最优；
- 研究“等势 + 力平衡”是否形成可证明的有限 `N` 结构。

## 6. 目标顺序

### 校准集

- `N=2`：精确值 `24/5`，用于验证内层最小化和对称性。
- 公开证书中的 `N=7,8,10,14,15,20,36`：覆盖对称、非对称、近聚簇和大改进实例。

### 真实攻击集

第一批：`N=3,4,5,6,9,13`。

- 小 `N` 可做更全面的全局多起点，也更可能形成精确猜想；
- `N=9,13` 是公开证书项目明确未改进的难点，击穿价值最高；
- 若只击穿官网但不超过公开值，不记为新纪录。

第二批：选择公开证书值相对 `pi*N*log N` 残差异常、对称性变化或近聚簇开始的 `N`，由数据而非感觉决定。

## 7. 严格验证

最终坐标导出后：

1. 把十进制文本解释为精确有理数；
2. 运行两种 fixed-configuration 连续下界证书；
3. 独立枚举数值局部最小点，给出上界 witness；
4. 检查 `lower_bound <= true_value <= upper_witness` 的区间宽度；
5. 对所有格式转换后重新验证；
6. 保存 verifier 源码 hash、坐标 hash、环境、耗时和完整 stdout/stderr。

严格下界只证明该构型，不证明全局最优。若研究小 `N` 的全局最优，需要额外对“所有源位置”做上界证书，这是另一个难度等级。

## 8. 研究数据表

每个 `N`、每个构型至少保存：

- 坐标与多重度/近簇标记；
- 数值最小值、严格下界、上界 witness；
- 所有数值局部最小点及 Hessian 特征值；
- active KKT 权重与平衡残差；
- 对称群分类；
- 最近点距离、Voronoi cell 面积、边/角距离；
- 与裁剪三角晶格、CVT 和公开 incumbent 的最优匹配距离；
- `P_N - pi*N*log N` 及候选尺度化残差；
- 搜索族、种子、GPU 时间、精化时间、证书时间。

持久数据放 `data/`，可删运行轨迹放 `runs/`，所有实验追加写研究日志。

## 9. 阶段 B 的预注册检验

先写检验，再看漂亮图，避免事后故事：

1. 用不同 `N` 窗口比较 `N`、`sqrt(N)`、`sqrt(N) log N` 等修正模型，以留出法评估，不凭单次拟合命名“次阶定理”。
2. 分开统计内部、边、角最暗点与源，检查边界层厚度是否按 `N^-1/2` 缩放。
3. 以局部 Delaunay 角度/配位数而非肉眼判断三角晶格。
4. 对近重合簇做分裂方向 Hessian 和高精度扰动测试，判断它是稳定结构还是优化器退化。
5. 在 `s` 邻域和物理正则核上延续同一盆地，寻找可重复的分岔点。

## 9b. 3D 游戏应用模型阶梯

不要直接跳到完整 path tracing。按可验证层级逐步增加现实因素：

1. 单位正方形、同平面、无阴影 `1/r^2`：数学基准与严格证书层。
2. 固定灯高 `h`，平面活动域，核 `1/(r^2+h^2)`：消除奇点并测试构型延续。
3. 水平 Lambert 接收面：加入 `h/(r^2+h^2)^(3/2)` 型入射余弦。
4. 引擎式 Attenuation Radius、不同亮度、禁放区和固定灯位候选。
5. navmesh/三角网格观察域；用 BVH/ray query 加二值或软 visibility。
6. 最后才加入材质、聚光方向、间接光或从引擎烘焙器采样的黑盒核。

建议应用原型输入一个 navmesh/采样表面、灯数、可放置域和引擎衰减参数，输出
灯位、最暗点与改善前后统计。评价用留出的高密度表面样本或真实引擎渲染采样，
不能只报告优化时网格。

## 10. 近期里程碑

- [x] 固定上游证书库版本并复现 `N=7` 两种精确证书。
- [x] 验证 CUDA Python 环境并实测 RTX 5090 吞吐。
- [x] 写独立 evaluator：网格、内部极小、四边极小、聚类与 Hessian。
- [x] 复现 `N=1` 精确值和公开 `N=7` 严格区间。
- [x] 得到并双重严格认证 `N=3` 下界 `7.56838963`。
- [x] 完成 `N=3` 全局证明 ROI 审查与完整六维局部二阶诊断。
- [ ] 实现 batched soft-min + exchange loop。
- [ ] 对 `N=3..6,9,13` 做固定种子预算相同的多族扫描。
- [ ] 只有在超过公开前沿并完成严格证书后，才准备对外材料。
