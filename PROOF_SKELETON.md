# N=3 最优性证明骨架

状态标签：`[数值确认]`、`[可严格化]`、`[待证]`、`[高风险]`。

## 0. 目标与候选

令

\[
I(A)=\min_{p\in Q}U(p;A),\qquad
U(p;A)=\sum_{i=1}^3\frac1{\lVert p-a_i\rVert^2},
\quad Q=[0,1]^2.
\]

高精度候选为

\[
a_1=(a,b),\quad a_2=(1-a,b),\quad a_3=(1/2,c),
\]

其中 `a,b,c` 的 100 位数值见 `data/candidates/n03_symmetric.json`。对该高精度
KKT 根，活跃最暗点是四角与 `(1/2,0)`，共同值记为 `t_*`。

已经严格证明冻结的 19 位有限小数构型满足

\[
7.56838963\le I(A)\le7.56838964002969021430.
\]

右端来自冻结字面小数坐标的角点 `(0,0)`。19 位截断使下边中点比该角点高约
`3.19e-19`；这不是高精度驻点的结构破坏，也不影响下界证书。这一固定构型上界
不是源位置空间的全局上界。

## A. 严格局部最优定理

### A1. 真正驻点的存在与唯一性 `[已严格证明]`

以未知量 `(a,b,c,lambda_T,lambda_C,lambda_M,t)` 写七个方程：

1. 顶角、底角、底边中点三类势等于 `t`；
2. 三个源参数方向的加权 KKT 梯度为零；
3. 三个轨道权重之和为 1。

数值根的最大残差约 `1.8e-100`。纯标准库、精确有理区间自动微分与 Krawczyk
算子已经证明：以该 100 位中心、统一半径 `1e-40` 的七维盒内恰有一个真根，
且三个权重严格为正。Krawczyk 像相对盒半径的最大位移约 `2.61e-38`。

### A2. 活跃最暗点局部化 `[可严格化]`

需要证明在候选源位置的一个邻域内：

- 四角保持严格受约束局部极小；
- 下边存在唯一随源位置光滑移动的内部边极小 `x_m(A)`；
- 正方形其余部分的势比这五个分支高一个统一正间隙。

当前数值法向梯度：顶左角约 `(19.06,-10.22)`，底左角约
`(10.41,28.17)`，底边中点的内法向导数约 `19.27`；符号均严格。
固定构型 exact branch-and-bound 可改为“排除五个小邻域后的下界”，给最后一条
统一间隙。边分支的唯一性由 `U_xx>0` 的区间界和隐函数定理得到。

### A3. 五分支 epigraph KKT `[数值确认 → 可严格化]`

定义四个角分支 `g_1,...,g_4` 和移动底边分支

\[
g_5(A)=U((x_m(A),0);A).
\]

局部问题等价于最大化 `t`，约束 `t<=g_j(A)`。五个严格正权重为

```text
top-left/top-right       0.3568414993507871 each
bottom-left/bottom-right 0.09995228102105895 each
bottom-midpoint          0.086412439256308
```

完整六维加权源梯度残差约 `4.5e-16`；活跃约束梯度秩为 4。加入 `t`
列后应验证 epigraph 的 LICQ/MFCQ 版本及乘子唯一性。

### A4. 移动最暗点的二阶包络 `[数值确认 → 可严格化]`

不能把 `(1/2,0)` 当固定见证点。对移动边极小，值函数 Hessian 为

\[
\nabla^2 g_5
=U_{AA}-U_{Ax}U_{xx}^{-1}U_{xA}.
\]

固定点版本在二阶临界子空间有一个 `+3.38` 方向；加入上述 Schur complement
后两个临界特征值约为

\[
-20.90257,\qquad -1.62793.
\]

因此标准非线性规划二阶充分条件有严格余量。可用有理区间 LDL/Sylvester
判据在 A1 的根盒上证明投影 Hessian 严格负定。

### A5. 局部结论 `[待证，低到中风险]`

A1--A4 完成后，由 epigraph 二阶充分条件可得：存在 `epsilon>0`，除源的排列外，
候选真根是 `I(A)` 的严格局部最大点。这一结论不需要覆盖全部六维源空间，ROI 高。

## B. 全局上界的计算机辅助路线

### B1. 源位置盒的有限见证上界 `[已证的基本引理]`

设第 `i` 个源被限制在矩形盒 `B_i`。对任意有限观察点集 `W`，定义

\[
\operatorname{UB}(B_1,B_2,B_3;W)
=\min_{p\in W}\sum_{i=1}^3\frac1{\operatorname{dist}(p,B_i)^2},
\]

若 `p in B_i`，相应项记为正无穷。则对盒内任意源构型 `A`，

\[
I(A)\le \operatorname{UB}(B_1,B_2,B_3;W).
\]

证明：连续域最小值不大于任意有限见证点处的最小值；而对固定见证点，
盒内源对倒平方项的最大值正是到该盒最短距离平方的倒数。这个界可完全用有理数
计算，不受奇点破坏；某见证点落入源盒只会让该见证失效，可换另一个点。

### B2. 覆盖、对称与分裂 `[已实现并验证]`

1. 用有理网格单元覆盖单源正方形；按单元编号排序三个源，除去 `3!` 排列重复。
2. 对每个三源盒用 B1 剪枝；未剪枝盒沿最敏感/最宽坐标二分。
3. 利用正方形 `D4` 对称选规范代表，或至少在结果层合并八个等价邻域。
4. 见证集包含有理边界网格，并从盒中心构型的数值最暗点生成有理化自适应见证。
5. 浮点版本只测剪枝率；最终所有“已覆盖/已剪枝”判定必须用 exact rational 或
   outward-rounded interval 重放。

### B3. 混合局部/全局证书 `[建议路线]`

- 候选邻域：用 A 部分的严格局部二阶上界处理，避免在最优点附近无限细分。
- 邻域外：用 B1/B2 证明统一严格间隙 `I(A)<t_*`。

这比单纯把六维空间细分到 `1e-8` 更可能收敛。

### B4. ROI 闸门 `[7.58 已通过]`

先用 float64 原型测试宽松目标：`P_3<7.7`。记录每层活动盒数、剪枝比例、
见证失效率和内存增长。只有宽松界在可控节点数内闭合，才依次尝试 `7.58`、
`7.57`；失败则停止全局证明，不影响局部定理和纪录项目。

实际结果：`7.7` 用 41,078 叶闭合；`7.58` 用 1,304,240 叶闭合并由独立
标准库验证器重放。因此当前全局夹逼为 `7.56838963<=P_3<=7.58`。在完成 A2--A4
前不盲目追求 `7.57`，因为候选邻域将成为纯细分法的主要复杂度来源。

## C. 已知不能直接使用的捷径

1. **固定五点对偶平均**：每个点源可落在见证点造成奇异，单个固定离散测度无法
   给全源空间统一上界；数值上五点离散游戏可达到约 `7.63>t_*`。
2. **把底边中点固定**：会给错误的正二阶方向，必须使用移动最暗点的包络 Hessian。
3. **稠密观察网格即证明**：它只给离散游戏上界；全局最大化本身仍未严格求解。
4. **GPU 即全局证明**：GPU 适合批量反例/候选；六维覆盖与 outward rounding
   才决定严格证明是否可行。
