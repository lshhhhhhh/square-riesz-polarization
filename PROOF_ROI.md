# N=3 全局最优证明：ROI 审查

状态：**严格局部最优闸门已通过；保留精确等式早停，评估 7.57 全局上界。**

## 1. 重要性

- 正面：这是一个干净的二维临界 Riesz polarization 小规模实例；若能给出首个
  `N=3` 全局定理或可复核计算机辅助窄区间，可作为有限尺寸理论和验证器方法的
  样板。
- 限制：它不是该领域的核心开放问题；单独一个小 `N` 精确常数的影响有限。
- 可复用产出：半无限 exchange、连续最暗点评估、固定构型精确证书、外层全局
  branch-and-bound，以及活跃点/KKT 数据都可迁移到其他 `N` 和其他核。

应用修正：点光源平方反比衰减确实存在于主流 3D 引擎；在无阴影平面活动域上，
“最大化最低照明”与本题同构。完整项目（可推广求解器、遮挡/高度/截断核和编辑器
工作流）的重要性因此为**中高**。但单独证明一个 `N=3` 精确常数仍为**中等**：
它是验证基准和理论锚点，不是游戏工具产生价值的必要前提。

## 2. 风险

目标需要证明

\[
\forall(a_1,a_2,a_3)\in([0,1]^2)^3\quad
\exists x\in[0,1]^2:\quad
\sum_i\lVert x-a_i\rVert^{-2}\le C.
\]

困难来自六维外层连续空间、内层最暗点切换、奇异核和 `forall-exists` 量词交替。
现有固定构型证书只解决固定六个坐标后的二维 `forall x`，不能直接升级。

风险评级：

- 数值找到更好构型：**低到中**；RTX 5090 很合适。
- 证明候选严格局部最优：**中**；活跃集、包络 Hessian 和边界法锥可处理。
- 证明 `P_3 <= C+1e-3` 的计算机辅助粗上界：**中到高**。
- 证明与候选完全相等或 `1e-8` 窄区间：**高**。

这不是黎曼猜想/`P=NP` 等级的已知深水区，但也绝不是固定构型证书的自然小改。

## 3. 工具匹配

- RTX 5090：适合批量 basin 搜索、对称破缺扰动、有限见证点反例搜索。
- float64/SciPy：适合活跃集、KKT、局部二阶和 exchange；不能给全局结论。
- 精确有理/区间 branch-and-bound：适合最终证书，但主要吃 CPU，且可能遭遇
  六维指数爆炸。
- AI：适合跨文献迁移、生成验证器和寻找反例；不能替代复杂度与证明审计。

对应用路线，GPU 的匹配度反而更高：真实 navmesh/表面会产生大量观察点，且要
同时优化许多灯位候选；这正是 RTX 5090 批量张量求值的强项。

## 4. 闸门与早停

1. 确认候选通过完整六维一阶 KKT、移动最暗点后的二阶必要条件和邻域反例搜索。
2. 多族、多种子搜索没有找到更高盆地。
3. 做外层上界原型，只要求证明一个放宽界（例如 `P_3 < 7.7`），测量每层剪枝率。
4. 若粗界无法在可控节点数内收敛，停止全局证明，保留纪录与局部结构结果。
5. 若粗界扩展良好，再依次收紧到 `7.58`、`7.57`，最后才讨论贴近候选的证书。

任何“全局最优”对外措辞，都必须等到覆盖所有源构型的严格上界完成。

## 5. 2026-08-19 闸门结果

- `P_3<=7.7`：41,078 叶精确证书经独立审计通过。
- `P_3<=7.58`：1,304,240 叶精确证书由加固后的标准库验证器完整重放通过。
- 结合固定构型下界，得到 `7.56838963<=P_3<=7.58`，相对缺口约 `0.153%`。
- 对称 KKT 根的半径 `1e-40` 纯有理 Krawczyk 包含通过，A1 的存在、唯一性与
  正权重已严格化。
- 活跃观察点隔离、五分支 epigraph LICQ、移动底边分支的包络 Hessian 与二维
  临界空间负定性均已用纯有理区间证书闭合；因此该真根在完整有序六维源空间中
  是严格局部最大点。

这说明工具足以处理“窄区间 + 局部结构”，ROI 为正。下一闸门是把局部二阶信息
转成候选邻域的显式上界，再测试全局覆盖能否从 `7.58` 压到 `7.57`；若邻域封口
仍导致节点爆炸，则保留当前局部定理与 `0.153%` 夹逼，不追求精确等式。
